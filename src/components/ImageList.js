import ImageItem from "./ImageItem";

function ImageList({ images }) {
  console.log(images);
  return (
    <div>
      {/* {images.map((img) => (
        <ImageItem />
      ))} */}
    </div>
  );
}

export default ImageList;
